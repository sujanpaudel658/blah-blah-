const {
  requestPayout,
  getPendingPayouts,
  approvePayout,
  rejectPayout
} = require('./payment.core');

module.exports = {
  requestPayout,
  getPendingPayouts,
  approvePayout,
  rejectPayout
};
